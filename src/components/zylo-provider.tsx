'use client';

/**
 * Zylo Provider Placeholder
 *
 * This is a placeholder component. Auth will be injected at runtime.
 */

export function ZyloProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
