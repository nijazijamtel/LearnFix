import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.learnfix.app',
  appName: 'LearnFix',
  webDir: 'out',
  bundledWebRuntime: false,
};

export default config;
